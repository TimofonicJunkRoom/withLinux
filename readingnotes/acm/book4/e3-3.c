/*
 * def toint(x):
 *   try:
 *     return int(x)
 *   except ValueError:
 *     return False
 *  vector = [ toint(item) for item in input.split() if toint(item) ]
 *  print( last 3 digits of prod(vector))
 */
